# 研发过程关键环节关注点

## 第 01 页

（2025年12月）

<table border="1"><tr><td>类型</td><td>检查项</td><td>具体检查项</td><td>检查方法与要点说明</td><td>建议检查阶段</td></tr><tr><td rowspan="2">账务处理</td><td>账务异常状态处理</td><td>交易超时或状态未明时，禁止直接判定失败，需优先通过查证或异步冲正机制确认结果，避免误操作引发资金风险</td><td>参考双十红线设计指引中的“账务异常状态处理设计模型”，检查程序对于异常状态的处理逻辑。要点：在交易未明或交易超时时必须获取必要的交易状态才能进行后续续作、重试、冲正等异常处置动作。</td><td>开发与代码审查阶段</td></tr><tr><td>联机交易防重与幂等</td><td>验证对同一笔核心业务请求（如支付、记账）的重复提交，是否通过token机制、幂等性设计等手段保证了只处理一次。防重需符合以下原则：1)交易防重应针对最终处理记录实现流水防重2)防重设计状态需设计中间状态，不允许只采用未处理和处理完成的双态设计;</td><td>联机交易：使用同一请求ID或业务数据重复调用接口，检查结果和数据库状态是否一致且只产生一笔有效记录。要点：“防重”是保证数据准确性的生命线。</td><td>开发与代码审查阶段</td></tr><tr><td rowspan="2"></td><td>批量定时任务防重机制</td><td>批量定时框架应具备“防重调”的功能，防止已经执行完成或正在执行的批量被重复调启。需符合以下原则：1)必须具备任务防重和交易防重，交易防重应针对最终处理记录实现流水防重；2)防重设计状态需设计中间状态，不允许只采用未处理和处理完成的双态设计；3)涉及自动化入账的定时或批量任务，如处理中的单笔业务记录异常，则该笔业务记录不允许自动化执行，应转人工处置；4)如涉及多个系统，因在接入层，中间层，服务方分别独立按以上原则独立实现防重设计，不允许只依赖单一节点进行防重控制。</td><td>1、检查批量程序是否有基于批量步骤和子步骤将步骤执行状态登记(开始、完成或失败)，程序启动时是否有对状态位的判断逻辑；
2、检查业务批量处理数据时是否有防重机制(重复数据校验、数据库唯一索引、行级锁等手段)；
2、测试同时触发多个实例时的处理机制；
要点：批量程序设计时不能只依赖批量调度框架本身的防重能力，在批量程序上增加防护机制有助于增加其健壮性。</td><td>开发与代码审查阶段</td></tr><tr><td>批量定时断点续作能力</td><td>批量定时系统设计时须在结构上或流程上考虑实现批量程序的重做或断点再续功能。</td><td>1、检查程序中的判断逻辑，是否有基于批量执行状态的判断逻辑，是否可以根据执行状态跳过或删除重新处理；
2、构造异常中断场景测试恢复流程。
要点：批量任务因设计无法重做或续做，导致下游系统处理或对外服务延迟。</td><td>开发与代码审查阶段</td></tr></table>

## 第 02 页

<div align="center">

# 研发过程关键环节关注点

</div>

（2025年12月）

<table class="table table-bordered"><thead><tr><th>类型</th><th>检查项</th><th>具体检查项</th><th>检查方法与要点说明</th><th>建议检查阶段</th></tr></thead><tbody><tr><td rowspan="2">批量定时设计</td><td></td><td>实现批量程序的重做或断点再续功能。</td><td>要点：批量任务因设计无法重做或续做，导致下游系统处理或对外服务延迟。</td><td></td></tr><tr><td>批量定时文件校验设计</td><td>系统在生成和处理批量定时文件时，建立必要的批前文件校验规则，提升对批量文件格式、内容等维度导致系统性风险的敏感度，文件校验规则划分为两类：<br>1、系统性风险校验规则：批量文件因格式、内容、数据等异常，存在对批量文件使用方系统造成大面积数据、功能异常，存在阻塞后续批量、联机业务的情况，制定的校验规则。<br>2、数据质量校验规则：批量文件存在部分数据质量问题，存在对批量文件使用方局部少量业务数据或后续批量、联机业务产生影响的情况，制定的校验规则。<br>（参考重要批量文件批前校验规则及生产风险预警机制方案）</td><td>1）文件名称与格式校验<br>a.文件名称校验：应确保约定文件命名规范统一，批前检查文件名是否符合指定的格式。<br>b.文件格式校验：验证文件包及文件类型是否符合要求，是否内容可正常读取（如XML格式），避免格式不兼容导致处理错误。<br>c.编码格式校验：应约定文件编码格式，批前检查可采用全量或采样方式进行编码格式校验。<br>注：文件名称与格式校验失败，会直接阻塞后续批量处理导致批量失败，应及时预警。<br>2）文件大小与数量校验<br>a.文件大小校验：应约定文件大小阈值，如最小或最大文件大小，批前检查文件超限情况。<br>b.文件数量校验：应约定文件数据量异常阈值，如0条有效数据或突增峰值，避免数据处理方导入异常或处理时长超预期。<br>c.空文件校验：应约定是否可为空文件及空文件格式（如换行符或其它不可见字符），通过文件大小或格式判断是否为空。<br>d.数据完整性校验：应约定就绪标识文件（如.ok），批量前进行就绪文件检查。<br>注：以上阈值应至少设计阈值下限值，是否设置上限或峰值预警值，根据具体业务场景实施。<br>3）字段唯一性与非空校验<br>a.字段唯一性检查：如有唯一性要求的关键字段，应约定进行唯一性校验。<br>b.非空值字段校验：如有必填要求的关键字段，应约定进行空值字段校验。<br>c.字段数量校验：对于多值域批量数据文件，应约定单务</td><td>开发与代码审查阶段</td></tr></tbody></table>

## 第 03 页

## 研发过程关键环节关注点

（2025年12月）

<table border="1"><tr><td>类型</td><td>检查项</td><td>具体检查项</td><td>检查方法与要点说明</td><td>建议检查阶段</td></tr><tr><td rowspan="3">数据备份</td><td>关键数据要备份</td><td>核心业务数据须同时满足数据库备份、业务文件归档及日志留存要求，任何环节故障均能恢复，确保零丢失</td><td>1、检查数据备份协议，是否包含正文备份和归档备份；
2、应定期进行数据恢复演练；
要点：数据备份是灾难发生的最后一道防线</td><td>测试与上线前验证阶段</td></tr><tr><td>变更数据备份先行</td><td>执行数据删除或迁移前，必须备份原数据并验证可用性，变更失败时可快速回滚。</td><td>1、检查变更脚本中delete、update语句前是否包含备份语句；
要点：养成好习惯，数据修改前先备份</td><td>测试与上线前验证阶段</td></tr><tr><td>Redis使用需谨慎</td><td>避免将Redis作为唯一数据存储，需同步落盘或设计降级方案，防止缓存异常导致数据不可恢复。</td><td>1、模拟缓存宕机测试降级方案
2、检查双写机制的数据一致性
要点：缓存数据必须可重建，且重建时间可控</td><td>开发与代码审查阶段</td></tr><tr><td rowspan="4">SQL规范</td><td>索引匹配高效可用</td><td>联机交易相关SQL必须建立高效索引，且查询条件需与索引列完全匹配，避免全表扫描</td><td>每月修改或新增的sql，需要逐一检查where条件对应的字段是否包含索引
要点：重点关注高频查询的索引效率</td><td>开发与代码审查阶段</td></tr><tr><td>谨防索引失效语法</td><td>1、检查是否存在导致索引失效的写法（如对索引列使用函数、类型隐式转换、like&#x27;%xx&#x27;左模糊等。
2、对于复合索引，SQL语句的查询条件需要遵循索引的最左匹配原则。如需对索引列进行排序时，按照复合索引组成列的顺序书写。</td><td>方法：1.在测试数据库对SQL执行EXPLAIN，观察type列是否为ALL（全表扫描）。2.与DBA一起评审涉及大表的复杂SQL。3.使用Everdb数据库的项目组可通过DAMP生产元数据信息查看生产表记录数情况。
要点：“大表”需各项目组根据自身情况自行定义明确。索引失效是性能问题的头号杀手。</td><td>开发与代码审查阶段</td></tr><tr><td>数据库字段初始值处理</td><td>数据库新增字段时，如设置非空（NOT NULL）且有默认值，需评估对存量数据的影响。同时分析应用代码中，读取该新字段时是否处理了“默认值”或“空值”情况。</td><td>方法：确认所有读写该字段的代码，其业务逻辑与“默认值”兼容；对应数据库是否为大表，可在测试环境模拟插入的效率是否会产生锁表、耗时长等情况。
要点：核心是影响分析，不仅要考虑数据库层面（新增字段对大表的影响等），还要考虑业务逻辑层面。</td><td>开发与代码审查阶段</td></tr><tr><td>分区分表设计规范遵从</td><td>1、数据表数据量大于1000万或超过4G，必须分区或分表；且分区表查询条件必须包含分区键。
2、参考研发十条分区设计指引，程序应实现定型分区分表。</td><td>1、定期对系统表的数据量进行重检，考虑是否需要做分表或分区；
2、对于分区表，检查SQL是否包含分区键条件；
要点：分区或分表策略应与表的大小、业务模式匹配</td><td>开发与代码审查阶段</td></tr></table>

## 第 04 页

## 研发过程关键环节关注点

（2025年12月）

<table border="1"><tr><td>类型</td><td>检查项</td><td>具体检查项</td><td>检查方法与要点说明</td><td>建议检查阶段</td></tr><tr><td></td><td></td><td>部分检查和分区扩展功能。</td><td></td><td></td></tr><tr><td rowspan="2">代码健壮性</td><td>变量赋值错误</td><td>在代码审查中，已对所有命名相似的变量（如userName/userNames）和拷贝粘贴的代码块、或者前端传输到后端字段大小写等进行重点核对。</td><td>方法：对粘贴来的代码，逐行检查其上下文是否适配当前逻辑，特别是修改变量名、条件判断、资源路径等。
要点：这是代码检查的必检项，不能仅靠开发者自检，建议交叉检查。</td><td>开发与代码审查阶段</td></tr><tr><td>定长字符串处理</td><td>1.对所有接口传入、数据库读写、文件处理的定长字段，在代码中显式进行长度判断与截断。
2.禁止依赖前端或调用方进行长度控制。（参考技术设计异常场景清单）</td><td>方法：1.在代码中搜索所有与定长数据库字段（如CHAR(10)，VARCHAR(50))映射的变量。
2.项目组可自定义定长字段截取方法，添加类似StringUtils.substring(str,0,maxLength)的逻辑，做相应的检查与截断。
要点：目标是防止因超长导致的入库失败、接口报错或文件格式错乱。</td><td>开发与代码审查阶段</td></tr><tr><td rowspan="3">测试场景覆盖</td><td>文件处理异常全场景覆盖</td><td>1.在测试案例中，已包含对空文件、文件格式错误、文件大小异常、文件部分缺失等异常场景的测试。
2.验证系统在遇到上述异常时，是否有明确的错误提示和日志，且不会导致系统崩溃或核心业务中断。</td><td>方法：1.构造各种异常文件进行测试。
2.检查日志，确认捕获了预期的异常。
要点：健壮性测试的关键，不能只测正常场景。</td><td>测试与上线前验证阶段</td></tr><tr><td>业务场景全覆盖测试</td><td>对于新增业务流程，测试要覆盖主流程、分支流程、备选流程（如应急登录）等；对于修改业务流程要进行影响性测试，重点关注修改点的位置，若修改点在主干流程，影响性测试要覆盖该主干流程下的所有分支，避免只选取部分分支。</td><td>测试案例应覆盖程序逻辑的所有情况，可对照详细设计方案逐一编写测试案例。
要点：技术测试是研发流程的重要一环，需求的测试要点应在详细设计环节就根据设计方案确定。</td><td>测试与上线前验证阶段</td></tr><tr><td>测试数据边界值和特殊值设计</td><td>1、在设计和选取测试数据时，关注边界值、特殊值、空值等。
2、测试数据需要考虑生产数据的真实情况，不可仅使用一些无业务意义的数据进行测试。</td><td>1、检查测试数据包含NULL/空字符串/极值
2、重要需求可获取生产数据进行测试
要点：测试数据设计是测试案例的重要部分，数据失真影响测试效果</td><td>测试与上线前验证阶段</td></tr><tr><td></td><td></td><td>1.在制定投产实施变更内容时，如涉及大表</td><td>方法：1.可以在变更管理流程中设置检查点，对此类变更</td><td></td></tr></table>

## 第 05 页

（2025年12月）

<table border="1"><tr><td></td><td>检查项</td><td>具体检查项</td><td>检查方法与要点说明</td><td>建议检查阶段</td><td></td></tr><tr><td></td><td></td><td>可仅使用一些无业务意义的数据进行测试。</td><td>响测试效果</td><td></td><td></td></tr><tr><td rowspan="5"></td><td>大表结构调整</td><td>1.在制定投产实施变更内容时，如涉及大表(记录数&gt;100万或数据量&gt;10G)的结构变更(如加字段、加索引)，必须经过DBA和应用管理员评审。2.评审内容必须包括：变更窗口、预计耗时、是否锁表、回滚方案、对应用的影响。</td><td>方法：1.可以在变更管理流程中设置检查点，对此类变更触发强制审批。2.提前与DBA沟通，是否有相关在线变更工具或者选择低交易量时间，或者使用临时表备份后再别名覆盖等方式，避免锁表。要点：沟通不是发个邮件通知，而是坐下来一起评审方案，确保风险可控。</td><td>投产变更管理阶段</td><td></td></tr><tr><td>环境配置排查</td><td>1.在投产前，使用自动化脚本或对比工具，对生产部署包内的配置文件(如.yml.properties)进行扫描。2.确认其中无任何开发、测试环境的IP、账号、密码、路径等配置。(参考投产前投产验证和生产环境应用配置差异比较分析框架)</td><td>方法：1.编写一个简单的脚本，grep搜索配置文件中是否包含dev,test,localhost等关键字。2.与标准的生产环境配置清单进行逐项比对。要点：这是最低级但后果最严重的错误，必须通过工具化解决人为疏忽。</td><td>测试与上线前验证阶段</td><td></td></tr><tr><td>投产验证测试需完善</td><td>技术及业务需求变更均要安排业务人员进行业务验证，验证案例要覆盖系统变更点及系统全链路，联机交易验证测试要做全，对于批量文件发生变化的要注意跟批，关注好定时任务。</td><td>检查投产验证测试案例，并与业务和运维沟通测试计划要点：投产验证应做到应测尽测</td><td>投产变更管理阶段</td><td></td></tr><tr><td>投产步骤不改变</td><td>1、投产前对投产方案进行充分评审；2、投产中不随意改变投产步骤。</td><td>1、投产前对对投产实施控制表进行充分评估，要充分理解各个步骤和配置的实际作用，不可经验主义。2、投产中不能随意变更、省略投产步骤，遇到问题需要充分讨论并上报。要点：投产步骤不随意改变，如需改变需上报领导。</td><td>投产变更管理阶段</td><td></td></tr><tr><td>投产不夹带</td><td>严禁未经评审的内容混入投产包，变更必须按流程逐级审批，杜绝“夹带私货”。</td><td>投产前进行严格的代码比对，确保没有因为疏忽而混入的代码要点：夹带投产不是技术问题，但会导致潜在技术风险，且严重违反行内各项规范</td><td>投产变更管理阶段</td><td></td></tr></table>
