{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://icebearx.ai/code-workspace/extension-manifest-v3.json",
  "title": "Code Workspace Extension Spec v1 manifest schema v3",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "extensionSpecVersion", "experimental", "id", "name", "description", "version", "entry", "entrySha256", "timeoutMs"],
  "properties": {
    "schemaVersion": { "const": 3 },
    "extensionSpecVersion": { "const": 1 },
    "experimental": { "const": true },
    "id": { "$ref": "#/$defs/id" },
    "name": { "type": "string", "minLength": 1, "maxLength": 100 },
    "description": { "type": "string", "minLength": 1, "maxLength": 60, "pattern": "^[^\\r\\n]+$" },
    "version": { "type": "string" },
    "entry": { "const": "init.js" },
    "entrySha256": { "$ref": "#/$defs/sha256" },
    "timeoutMs": { "type": "integer", "minimum": 1, "maximum": 300000 },
    "capabilities": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "networkHosts": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/hostname" }
        }
      }
    },
    "outputs": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "kind", "ownership", "target"],
        "properties": {
          "id": { "$ref": "#/$defs/id" },
          "kind": { "enum": ["file", "directory", "text-block", "json-member"] },
          "ownership": { "enum": ["exclusive", "shared", "seeded"] },
          "target": { "$ref": "#/$defs/relativePath" },
          "selector": { "type": "string", "pattern": "^/(?:[^~/]|~[01])+(?:/(?:[^~/]|~[01])+)*$" },
          "format": { "enum": ["text", "toml"] },
          "tools": {
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "items": { "enum": ["claude", "codex"] }
          }
        },
        "allOf": [
          {
            "if": { "properties": { "kind": { "const": "file" } } },
            "then": { "properties": { "ownership": { "enum": ["exclusive", "seeded"] } } }
          },
          {
            "if": { "properties": { "kind": { "const": "directory" } } },
            "then": { "properties": { "ownership": { "const": "exclusive" } } }
          },
          {
            "if": { "properties": { "kind": { "enum": ["text-block", "json-member"] } } },
            "then": { "properties": { "ownership": { "const": "shared" } } }
          },
          {
            "if": { "properties": { "kind": { "const": "json-member" } } },
            "then": { "required": ["selector"] },
            "else": { "properties": { "selector": false } }
          },
          {
            "if": { "properties": { "kind": { "const": "text-block" } } },
            "then": { "properties": { "format": { "enum": ["text", "toml"] } } },
            "else": { "properties": { "format": false } }
          }
        ]
      }
    },
    "hooks": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "event", "command"],
        "properties": {
          "id": { "$ref": "#/$defs/id" },
          "event": { "enum": ["task.started", "task.activity", "write.before", "write.after", "task.turn-ended", "task.ended", "task.subagent-started", "task.subagent-ended", "session.start", "session.activity", "session.end", "turn.end", "subagent.start", "subagent.end", "pre-write", "post-write"] },
          "command": { "type": "string", "minLength": 1, "maxLength": 4096, "pattern": "^[^\\r\\n]+$" },
          "tools": {
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "items": { "enum": ["claude", "codex"] }
          },
          "matcher": { "type": "string", "minLength": 1, "maxLength": 1024, "pattern": "^[^\\r\\n]+$" },
          "timeoutMs": { "type": "integer", "minimum": 1, "maximum": 300000 }
        }
      }
    },
    "runtime": {
      "$ref": "#/$defs/runtime"
    }
  },
  "anyOf": [
    { "required": ["outputs"], "properties": { "outputs": { "minItems": 1 } } },
    { "required": ["hooks"], "properties": { "hooks": { "minItems": 1 } } },
    { "required": ["runtime"] }
  ],
  "$defs": {
    "id": { "type": "string", "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$" },
    "sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "hostname": { "type": "string", "pattern": "^(?=.{1,253}$)[a-z0-9](?:[a-z0-9.-]*[a-z0-9])?$" },
    "relativePath": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(?!/)(?!.*\\\\)(?!.*(?:^|/)\\.{1,2}(?:/|$))(?!.*//).+$"
    },
    "runtime": {
      "type": "object",
      "additionalProperties": false,
      "required": ["runtimeProtocolVersion", "entry", "entrySha256", "scope", "mode", "timeoutMs"],
      "properties": {
        "runtimeProtocolVersion": { "type": "integer", "minimum": 1 },
        "requires": {
          "type": "array",
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/id" }
        },
        "entry": { "$ref": "#/$defs/relativePath" },
        "entrySha256": { "$ref": "#/$defs/sha256" },
        "scope": { "enum": ["workspace", "global"] },
        "mode": { "enum": ["oneshot", "service"] },
        "timeoutMs": { "type": "integer", "minimum": 1, "maximum": 300000 },
        "maxOutputBytes": { "type": "integer", "minimum": 1024, "maximum": 4194304 },
        "service": {
          "type": "object",
          "additionalProperties": false,
          "required": ["id", "compatibilityGroup", "singleton"],
          "properties": {
            "id": { "$ref": "#/$defs/id" },
            "compatibilityGroup": { "$ref": "#/$defs/id" },
            "singleton": { "const": "user" }
          }
        }
      },
      "allOf": [
        {
          "if": { "properties": { "mode": { "const": "oneshot" } } },
          "then": {
            "properties": { "service": false }
          }
        },
        {
          "if": { "properties": { "mode": { "const": "service" } } },
          "then": {
            "required": ["service"],
            "properties": { "maxOutputBytes": false }
          }
        }
      ]
    }
  }
}
